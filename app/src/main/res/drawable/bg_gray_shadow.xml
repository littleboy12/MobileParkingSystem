<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow Layer -->
    <item android:top="100dp" android:left="100dp" android:right="100dp" android:bottom="100dp"> <!-- Offset shadow -->
        <shape>
            <corners android:radius="12dp" />
            <solid android:color="#FF0000" /> <!-- Warna bayangan -->
        </shape>
    </item>

    <!-- Button Background -->
    <item>
        <shape>
            <corners android:radius="12dp" />
            <solid android:color="@color/bg_gray" /> <!-- Warna tombol -->
        </shape>
    </item>
</layer-list>